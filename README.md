# 111plan

Static bilingual website for the Human-AI Collaborative Design 111 Center.

The page renders the latest exported Figma SVGs directly for pixel-level visual fidelity, with transparent HTML hotspots layered above the top navigation for section jumps and language switching.

Open `index.html` locally or publish the repository with GitHub Pages from the `main` branch root.
